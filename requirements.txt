# Pinned for reproducibility (Infra task S2). No floating ranges.
pyyaml==6.0.2

# Ingest (M1/M3/M4): no fallback path -- required for extract/chunk/embed to run.
pymupdf==1.28.2
pymupdf4llm==1.28.2
tiktoken==0.13.0

# Retrieval + ingest embeddings (A2/M4). Replaced sentence-transformers so query
# and stored vectors are guaranteed to come from the same library/model.
fastembed==0.8.0

# Generation: Ollama and/or Azure OpenAI / AI Foundry over HTTP.
requests==2.34.2

# Web UI (heinzy/webui). Prefer: pip install -e ".[webui]"
flask==3.0.3

# --- Optional, Chroma HTTP thin client for shared vector store (S4):
# chromadb-client==1.5.9
# Or: pip install -e ".[store]"
# (Use thin client — full `chromadb` needs MSVC build tools on Windows.)


